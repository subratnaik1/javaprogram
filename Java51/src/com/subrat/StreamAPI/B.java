package com.subrat.StreamAPI;

import java.util.stream.Stream;

public class B {
void main() {
//	Stream<Integer>s1=Stream.iterate(1, n->n+2);
//	s1.limit(10)
//	.map(n->n*n)
//	.forEach(IO::println);
	
//	Stream<Integer>s1=Stream.generate(()->(int)(Math.random()*1000));
//	s1.limit(10)
//	.filter(n->n%2!=0)
//	.forEach(IO::println);
}
}
